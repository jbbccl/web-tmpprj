# 复制成 .env 后按需修改：cp .env.example .env
# 不建 .env 也能跑，compose.yaml 里内置了同样的默认值。

# 数据库
DB_NAME=dbtmp
DB_USER=dbtmp
DB_PASSWORD=Dst123!@#
DB_ROOT_PASSWORD=rootpass
DB_PORT=3306

# 对外端口
BACKEND_PORT=8001
FRONTEND_PORT=5509
